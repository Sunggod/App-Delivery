import {COLORS, FONT_SIZE} from  "../../constants/theme"
export  const styles = {
    btn:{
        width:"100%",
        backgroundColor: COLORS.red
    },
    text:{
        fontSize: FONT_SIZE.md,
        color: "white",
        padding: 10,    
        textAlign: "center"
    }
}
